# AEQ-ALERT-DB-002 — database saturation

Check pool wait, database CPU/IO, timeline locks, long transactions, worker/bulk load, and reserved
administrative capacity. Shed optional work before increasing pools. Tune query/index/transaction
shape from evidence; never exceed the Part 45 connection budget. Verify sync latency and pool wait
recover together.

