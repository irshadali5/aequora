# AEQ-ALERT-SYNC-003 — sync availability burn

Confirm the SLI includes only eligible accepted, duplicate, and infrastructure-result classes.
Segment by bounded region/release/route dimensions, inspect DB and authority health, then compare the
current release with baseline. Client validation, authorization, conflicts, business rejection, and
offline duration are not server availability failures. Verify both fast and slow burn have recovered.

