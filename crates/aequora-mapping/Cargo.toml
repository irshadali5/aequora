[package]
name = "aequora-mapping"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Explicit physical schema mapping validation for optional Aequora record sync"
repository.workspace = true

[dependencies]
aequora-schema = { version = "0.1.1", path = "../aequora-schema" }
serde.workspace = true
thiserror.workspace = true

[lints]
workspace = true
